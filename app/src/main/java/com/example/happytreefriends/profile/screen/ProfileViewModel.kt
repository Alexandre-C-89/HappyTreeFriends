package com.example.happytreefriends.profile.screen

import androidx.lifecycle.MutableLiveData
import dagger.hilt.android.lifecycle.HiltViewModel
import kotlinx.coroutines.flow.MutableStateFlow
import kotlinx.coroutines.flow.StateFlow
import javax.inject.Inject

@HiltViewModel
class ProfileViewModel @Inject constructor(

) {

    /**private val _uiData = MutableStateFlow(OnboardingUiData())
    val uiData: StateFlow<OnboardingUiData> = _uiData

    private val _uiState = MutableStateFlow<OnboardingUiState>(OnboardingUiState.Idle)
    val uiState: StateFlow<OnboardingUiState> = _uiState

    val loginStateData = MutableLiveData<AccountLoginState>()*/

}